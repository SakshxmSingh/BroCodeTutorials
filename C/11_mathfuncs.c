#include <stdio.h>
#include <math.h>

int main(){

    double A = sqrt(9);                 //square root
    double B = pow(2, 4);               //power/exponential
    int C = round(3.14);
    int D = ceil(3.14);                 //round up
    int E = floor(3.99);                //round down
    double F = fabs(-100);              //absolute value
    double G = log(3);
    double H = sin(45);
    double I = cos(45);
    double J = tan(45);
    
    printf("\n%lf", A);

    return 0;
}